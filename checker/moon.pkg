import {
  "moonbit-community/bashcheck/parser" @parser,
  "moonbit-community/bashcheck/semantics" @semantics,
  "moonbit-community/bashcheck/checks" @checks,
  "moonbit-community/bashcheck/fixer" @fixer,
}

import {
  "moonbitlang/core/debug",
  "moonbitlang/core/test" @test,
} for "test"

import {
  "moonbitlang/core/debug",
} for "wbtest"

supported_targets = "+native"
